name: Hello World CI

on: [push]   

jobs:
  build:
    runs-on: ubuntu-latest  
    steps:
      - name: Checkout code
        uses: actions/checkout@v3  

      - name: Run Hello Script
        run: python hello.py
